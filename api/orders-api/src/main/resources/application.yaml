spring:
  application:
    name: orders
server:
  port: 0
  servlet:
    context-path: /orders
logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    com.smuralee: DEBUG
    org.hibernate: ERROR

app:
  features:
    secretManagement: false